$( function() {
						
				$.CBPQTRotator.defaults = {
				// default transition speed (ms)
				speed : 1000,
				// default transition easing
				easing : 'ease',
				// rotator interval (ms)
				interval : 6000
				};

				$( '#cbp-qtrotator' ).cbpQTRotator();

} );